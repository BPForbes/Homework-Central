# Copy this file to %UserProfile%\.wslconfig, then run `wsl --shutdown` and
# restart Docker Desktop. This cap is shared by Docker Desktop and every WSL 2
# distribution, including Ubuntu and Kali.
[wsl2]
memory=4GB
processors=4
swap=2GB

[experimental]
autoMemoryReclaim=gradual
