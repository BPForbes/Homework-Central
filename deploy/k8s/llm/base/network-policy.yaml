apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: llm
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: llm
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 11434
