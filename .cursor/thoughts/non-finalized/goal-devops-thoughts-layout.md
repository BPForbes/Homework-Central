# Goal: DevOps thoughts layout and async roles

**Role:** Orchestrator
**Status:** Open
**Done when:** Skill, agents, gitignore, and `docs/` index match
`.cursor/thoughts/{non-finalized,finalized}`; former research dumps
are out of `docs/`; every role is async (`is_background: true`);
human-interrupt side sprints and ask-paths are documented; QA PASS
then move this file to finalized.

## Non-goals
- Changing product C# / TS / Rust behavior
- Deleting durable `docs/tickets.md`, `identity.md`, `chat.md`, or the
  Comment Documentation Guide

## Handoff
- From: Orchestrator
- To: Coder
- Pass-along: The Client asked for thoughts dirs, gitignore swap,
  async agents, ask-paths, and side sprints from research on interrupt.
- Sent back because: n/a
- Ask: n/a
