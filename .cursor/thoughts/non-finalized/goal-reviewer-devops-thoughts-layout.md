# Goal: Reviewer — thoughts layout

**Role:** Reviewer
**Status:** Both reviewers Satisfied (keepfile contract holds). Ready
for Security. Satisfied does not authorize a push.
**Done when:** Satisfied that the skill and agents match The Client's
layout, ask-paths, and async spawn rules.

## Handoff
- From: Coder
- To: Reviewer
- Pass-along: Review thread is
  `.cursor/thoughts/non-finalized/review-devops-thoughts-layout.md`.
  Ask the Orchestrator if the Team Lead must decide.
- Sent back because: n/a
- Ask: n/a

## Handoff
- From: reviewer-2
- To: Orchestrator
- Pass-along: **Both reviewers Satisfied** at HEAD `536a8bf`.
  reviewer-2 keepfile contract holds; leftover-reviews ignore stays a
  suggestion only. Next gate: Security → QA. After PASS, move the
  eight `*devops-thoughts-layout*` Markdown files listed in
  review-thread round-2 Questions; keep
  `.cursor/thoughts/non-finalized/.gitkeep` tracked. Satisfied does
  **not** authorize a push. Only QA may give the OK to push.
- Sent back because: n/a
- Ask: n/a
