# Tells bailey-forbes.com that Homework Central changed, so it refreshes the
# metadata it displays and redeploys.
#
# Waits for CI rather than firing on push. Homework Central publishes no site of
# its own, so there is no deployment to wait for, but the portfolio prints its
# repository statistics and language balance and should not advertise a commit
# that does not build and test.
#
# The workflow_run `branches` filter already excludes pull requests from feature
# branches; the explicit `event == 'push'` check below also excludes the rare
# pull request opened *from* main.
#
# The dispatch itself lives in the portfolio repository so all projects share
# one implementation — see
# BPForbes/Bailey-Forbes-Portfolio-Site.github.io/docs/project-metadata.md.

name: Notify portfolio

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]

permissions: {}

concurrency:
  group: notify-portfolio
  cancel-in-progress: false

jobs:
  notify:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push'
    uses: BPForbes/Bailey-Forbes-Portfolio-Site.github.io/.github/workflows/notify-portfolio.yml@main
    with:
      project: homework-central
      sha: ${{ github.event.workflow_run.head_sha }}
      ref: ${{ github.event.workflow_run.head_branch }}
      source-workflow: ${{ github.event.workflow_run.name }}
    secrets:
      PORTFOLIO_DISPATCH_TOKEN: ${{ secrets.PORTFOLIO_DISPATCH_TOKEN }}
